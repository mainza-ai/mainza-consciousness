# Lightweight requirements for CI/CD Docker builds
# Core FastAPI dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-ai>=0.2.0

# Database and caching
neo4j>=5.11.0
redis>=4.5.0

# HTTP and networking
requests>=2.31.0
httpx>=0.24.0

# Security and authentication
python-jose>=3.3.0
python-dotenv>=1.0.0
bcrypt>=4.0.0

# Basic utilities
cachetools>=5.3.0
psutil>=5.9.0

# Lightweight data processing (no heavy ML)
pandas>=2.0.0
numpy>=1.24.0

# RAG Memory System (lightweight versions)
neo4j-graphrag>=0.1.0
ollama>=0.1.0

# Note: Heavy ML packages (torch, transformers, scikit-learn, etc.) 
# are excluded for CI/CD to avoid disk space issues
