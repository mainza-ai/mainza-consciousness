# Official LiveKit Ingress configuration
api_key: devkey
api_secret: supersecretdevkey1234567890abcdef
# Use container name for Docker networking
ws_url: ws://mainza-livekit:7880
redis:
  # Use container name for Docker networking
  address: mainza-redis:6379
# Required ports as per official docs
rtmp_port: 1935
whip_port: 8080
health_port: 8082
http_relay_port: 9090
logging:
  level: debug
rtc_config:
  # Official WHIP UDP port
  udp_port: 7891
  use_external_ip: false

